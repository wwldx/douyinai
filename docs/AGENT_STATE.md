# Agent State

更新时间：2026-07-20
状态：012 第三轮真正前端重构交接已准备；等待在同一项目下启动全新 Kimi 3 会话并完成旗舰路径设计检查点；CloudBase 公网仍为 011

## 当前目标

赛道四「视觉搜索」冰箱晚餐 Agent：从抖音 Feed 菜图或真实冰箱出发，结合用户确认库存、餐次和时间，给出今晚能否照着做、还差什么和可执行步骤。

当前唯一在途版本为 012 第三轮展示端重构。第一、二轮界面只作 Git 回滚和功能库存，不再作为设计基础。第三轮不冻结颜色、页面数量、入口形态、信息顺序、组件和后端能力上限；现有功能真实性、失败降级和 CloudBase 011 线上基线仍必须可追溯。升级状态以 `docs/roadmap/大区赛升级总计划-2026-07-13.md` 的 U34 为准。

## 发布与 Git 基线

- 当前公网：CloudBase 011，`https://fridge-dinner-agent-281751-9-1304313771.sh.run.tcloudbase.com/`，三任务均为 `gpt-5.6-terra`，Case 检索 `off`，011→010→011 回滚彩排已通过。
- 011 本地 Git 检查点：`00ee820 Checkpoint CloudBase 011 release candidate`。
- 012 第一轮 Git 检查点：`00e5fc3 Refactor showcase frontend for release 012`。
- 012 第二轮 Git 检查点：`fb4062f Checkpoint Kimi API frontend redesign`；工作分支仍为 `codex/frontend-012`，不推送、不部署，直到用户决定进入 CloudBase 012 发布流程。
- 第二轮补购语义修复与第三轮回滚基线：`b743c2c Clarify simulated shopping replan results`。
- 011 部署包、Skill、海报和备用视频继续作为当前正式制品；012 尚未重新打包这些资产。

## 012 第一、二轮已完成（历史回滚基线）

1. 保留 Vite + React 与现有双入口，把首页改成普通用户可直接理解的“想吃这道菜 / 看看冰箱”。
2. 菜图、冰箱、库存和结果页统一为“一屏一个问题、一个主操作”；示例入口降级，相机与相册仍分开。
3. 库存选中态增加勾选与 `aria-pressed`；局部补拍保持两按钮一行，优先处理继续按用户确认信息展开。
4. 结果页先显示结论、时间/难度/缺口和四步做法；材料摘要合并，方案历史、先吃清单和冰箱整理改为渐进展开。
5. 做饭救援、反馈、模拟补购重规划、最近 3 个方案、饭后生活记录均保留；失败诊断编号移入“查看详情”。
6. 视觉收敛为暖白、品牌绿和强调橙；关键触控至少 44px，加入 `focus-visible`、`prefers-reduced-motion` 和 iOS safe area。
7. 实际浏览器验收发现跨步骤保留旧滚动位置，已定向修复为阶段切换后回到页首。
8. Kimi Code K3 High 在 VS Code 中完成纯展示层第一版；Codex 随后审计交互合约、修复条件渲染、内部措辞、移动布局和滚动问题。
9. 第二轮改用 Kimi Platform API 的 `moonshot-cn/kimi-k3` High，新建干净会话重建 `index.css` 视觉系统，并统一入口、菜图、冰箱、库存、结果、救援、反馈和生活记录。Codex 随后恢复抖音商城闭环文案、去掉重复步骤提示并修正强调色对比度；未改变业务状态、API 或降级合约。

实施约束见 `docs/frontend/012前端重构实施约束-2026-07-20.md`。

## 012 第三轮设计交接

- 唯一设计权威：`docs/frontend/012第三轮真正前端重构设计简报-2026-07-20.md`。
- 新会话开场与续接 Prompt：`docs/frontend/012第三轮-Kimi3新会话开场Prompt-2026-07-20.md`。
- 新会话必须裸启动 Kimi，不能使用 `-c`、`-S`、`--session` 或旧 session 恢复命令。
- 独立设计概念形成前，不读取旧 `index.css`、旧展示 DOM、旧截图或第一、二轮实施约束；方向形成后才把旧实现当作功能/API 证据。
- 第一检查点只完成一个明确设计命题和一条高保真旗舰路径，用户在本地浏览器确认后再迁移全部功能。
- 现有 API 是能力基线而非上限。Kimi 可以提出或实现增强比赛呈现的新前后端能力；需要 Codex 接力时必须留下用户价值、接口/字段、失败兜底和实现状态。
- 不预设颜色、背景、页面数量、导航、信息顺序、组件、DOM 或 CSS；“即使移除颜色也能看出重构”是结构验收门槛。

## 冻结合约

- 未修改 `demo/`、`data/`、Docker、部署配置、API 路径、请求体、响应字段或模型配置。
- 相机 input 继续使用 `capture="environment"`，相册 input 不带 `capture`。
- 图片压缩、ROI、语音失败回文本、真实失败不冒充缓存、局部补拍合并、人工确认库存、模拟补购重规划、方案历史、两轮救援和生活记录编辑行为保持不变。
- 用户页正常成功时不展示模型/provider/Schema；网络失败时仍明确显示稳定方案，诊断编号可展开查看。

## 本地 V2 证据

- 第二轮最终 `npm run showcase:build` 通过；`git diff --check` 通过。
- 使用 `AGENT_APP_VERSION=012-local` 的无密钥生产服务验收：`hasApiKey=false`、语音云 provider 关闭、`agentRuns=false`、Case 检索 `off`、演示视觉缓存开启；没有触发外部模型。
- Codex in-app Browser 桌面固定 Feed 路线通过：黄焖鸡示例 → 示例冰箱 → 8 项库存 → 目标菜稳定结果；入口和结果首屏无横向溢出，当前视口可见按钮至少 48px。
- 精确 `390×844` 固定 Feed 路线通过：首页、菜图、冰箱、库存和目标菜结果均无横向溢出；库存按钮至少 44px，补拍两按钮同排，底部操作未遮挡内容。
- 精确 `390×844` 固定冰箱路线通过：示例冰箱 → 8 项库存 → “帮我决定” → 一锅番茄土豆蛋花汤稳定结果；无横向溢出。
- 目标菜结果点击模拟补齐后会生成第二份方案，明确区分“家里已有”和“本次模拟补入 · 尚未购买”，并可在方案记录中切回补齐前结果。
- 控制台仅有无密钥测试预期的本地 `/api/plan-target-dish` 与 `/api/plan-dinner` 503；前端均正确进入确定性 fallback，无页面运行时异常。
- 两条路径最终页面控制台无 error/warn；Browser 控制层自身的 Statsig 网络超时不属于应用页面错误。
- 本轮严格按 V2：未跑 30 例模型回归、未跑无关后端全量、未打包、未部署、未做公网调用。

## Kimi API 历史会话

- 官方 Kimi Code CLI `0.28.0` 已安装到 `~/.kimi-code/bin/kimi`，配置与 VS Code 插件共享 `~/.kimi-code`；不得同时运行同一会话。
- 第二轮 API 会话：`session_05893447-db3a-4575-8a13-6ca2e792a8b1`。第三轮明确禁止恢复该会话，避免继承旧提示造成视觉与结构锚定。
- 若开放平台余额耗尽，可用 `/login` 切换 Kimi Platform API Key；第三轮进度以新会话、Git 检查点和工作区文件为准，不依赖第二轮模型隐藏状态。
- 本轮会话记录为 24 次请求、99,070 uncached input tokens、2,528,512 cached-read tokens、29,668 output tokens；费用只以 Kimi Platform 账单为准。API Key 不写入项目或聊天记录。

## 下一步

1. 在项目根目录裸启动一个全新 Kimi 3 会话，发送第三轮开场 Prompt；不要继续第二轮 session。
2. Kimi 先完成独立设计命题和旗舰路径第一检查点，本地 build 后停止；用户在浏览器直接批注。
3. 方向确认后，同一新 Kimi 会话迁移另一类任务、次级能力和必要的新后端契约；Codex 定向补齐并审计功能真实性、失败降级和接口迁移。
4. 只有第三轮本地双任务验收通过后，才决定 CloudBase 012 打包、部署和 iPhone 真机矩阵；当前不推送、不部署、不切线上流量。

## 固定边界

- 创意比赛阶段优先呈现、稳定和可诊断性，不主动扩张产品级隐私工程。
- 仍不保存 API Key、管理员令牌、认证头、原始照片或原始音频。
- 不自动判断过期、变质、气味、熟度或是否安全食用；用户确认始终是前置条件。
- H5 的 `capture="environment"` 只能请求后置相机，不能控制 iOS 系统文件选择菜单。
