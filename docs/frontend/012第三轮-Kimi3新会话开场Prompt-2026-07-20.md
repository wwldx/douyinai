# 012 第三轮：Kimi 3 新会话开场 Prompt

日期：2026-07-20  
用途：在同一项目目录启动一个全新的 Kimi 3 会话，完成不受旧视觉锚定的前端重构

## 1. 启动方式

在项目根目录执行：

```bash
cd "/Users/wwl_mac/Desktop/2026春季第四学期/2026抖音ai创变者计划"
KIMI_MODEL_THINKING_EFFORT=high /Users/wwl_mac/.kimi-code/bin/kimi
```

不要使用 `-c`、`-S`、`--session` 或旧会话恢复命令。裸启动会在当前项目下创建新会话。

确认模型为当前配置的 `moonshot-cn/kimi-k3` 后，把下面整段作为第一条消息发送。

## 2. 第一条消息

```text
你现在是“冰箱晚餐 Agent”012 第三轮前端重构的首席产品设计师兼前端实现者。这是一个全新会话；不要恢复、引用或延续任何旧 Kimi 会话。

这次任务不是给现有页面换色或整理 CSS，而是从产品任务出发，真正重新设计用户体验和视图层。当前 012 页面只是一份已经接通的功能库存与 Git 回滚点，不是设计稿，也不是你需要保持连续性的基础。

先阅读：
1. AGENTS.md —— 只提取产品事实、真实能力、API/安全边界和比赛场景，不把其中对当前 UI 的描述当成设计规范。
2. docs/frontend/012第三轮真正前端重构设计简报-2026-07-20.md —— 本轮唯一设计权威。
3. package.json、frontend/src/main.jsx、frontend/src/data/samples.js，以及简报列出的可用 assets。

形成独立设计概念之前，不要读取：
- docs/frontend/012前端重构实施约束-2026-07-20.md
- docs/AGENT_STATE.md 中对第二轮视觉的描述
- frontend/src/index.css
- frontend/src/App.jsx 的展示 DOM
- assets/submission/screenshots/
- 旧 Kimi session 内容

这些旧内容会产生视觉和结构锚定。方向形成之后，你才可以读取 App.jsx 和相关组件；届时只把它们当作状态、事件、API 与边缘功能清单，不能据此退回旧页面结构。

你拥有完整设计自由，包括但不限于：
- 自己决定品牌视觉、色彩、背景、字体、图形、影像和动效。
- 自己决定页面数量、任务入口、导航、流程组织和返回机制。
- 自己决定信息顺序、结果表达、主行动和次级能力的位置。
- 自己决定组件体系和展示层代码结构。
- 允许删除旧展示 DOM、重写 CSS、拆分或重写 App 的视图层。
- 不要求沿用入口卡片、步骤条、结果卡、胶囊标签、固定底栏或任何当前组件形态。

不要把 React 当作审美限制，也不要为了显得重构而无意义更换框架。当前工程仍使用 Vite + React、构建输出 frontend/dist、API 同源；这些只是工程接口，不是视觉限制。确有必要时可以添加小型且兼容的前端依赖，但不要引入与体验无关的重框架。

必须保留的是用户能完成的事情，而不是旧页面：
- 从想吃的菜出发：菜图相机/相册/示例、手动 ROI、菜名人工确认、语音/文字约束、冰箱输入、库存确认、是否能做与缺料/步骤结果。
- 从冰箱出发：冰箱相机/相册/示例、库存人工确认、自由决定或补充目标菜、生成现实可做的一餐。
- 共同能力：等待与耗时、诚实失败降级、调整条件、重新规划、另一方案、方案历史、显式反馈重规划、先吃清单、冰箱整理、模拟补购重算、两轮做饭救援、饭后生活记录草稿。
- 模拟补购必须清楚区分家里已有、模拟补入和尚未真实购买；不能再让用户误以为已经下单。
- 不能自动判断过期、新鲜度、气味、熟度或安全食用；不能暗示真实购买、扣款或发布。
- 相机、相册、文本兜底、已经验证的用户能力和失败不伪装缓存必须保留。

现有 API 和后端不是设计上限：
- 现有端点只代表已经可用的能力基线，不要求新体验迁就它们的字段或页面结构。
- 如果你认为新增功能、状态、字段、接口或后端 Agent 能明显增强评委可见价值，可以直接设计并实现。
- 第一检查点尚不需要完整后端时，可以先使用稳定数据做出真实交互，并留下“用户价值 → 建议接口/字段 → 失败兜底 → 待 Codex 实现项”的精确契约。
- 不要因为后端暂时没有某项能力就把大胆设计退回旧结构；也不要为了显得创新而堆与核心故事无关的功能。

本次第一检查点不要一次迁移全部旧功能。按以下顺序工作：

A. 体验建模
- 用不超过 10 条要点写出一个明确设计命题、新的信息架构和最关键的交互机制。
- 只给一个你最有把握的方向，不要给三套泛化风格方案让我选择。
- 明确评委在 30 秒内如何理解产品，以及哪个瞬间最能证明“内容种草 → 现实库存 → 可执行决定”的价值。
- 这部分要短，目的是指导实现，不是写长报告。

B. 旗舰路径原型
- 直接实现一条高保真、可交互的旗舰路径，优先表现“刷到想吃的菜 → 结合冰箱 → 得到今晚能行动的决定”。
- 使用稳定示例数据和现有素材即可；第一检查点不要调用真实模型。若旗舰体验需要新后端能力，可以同步实现，或先完成前端交互与精确接口契约。
- 新设计必须从结构和交互上与 012 有实质区别，不能主要靠 CSS 换色。
- 优先新建独立的新视图结构，不要在旧 DOM 上逐卡片修补；具体文件组织由你决定。
- 手机体验和桌面演示都要有设计意识，不能只把同一列机械拉宽。

C. 停止条件
- 完成旗舰路径、一次 frontend 构建和必要的本地预览后停止。
- 不要在视觉方向未经用户确认前迁移所有次级功能或跑全量回归；可以为旗舰体验增加必要的后端能力，但不要部署、提交或推送。
- 最终告诉我：设计命题、改动文件、本地预览入口、旗舰路径如何操作、暂未迁移的功能，以及需要新增或已经实现的后端契约。

工作纪律：
- API 额度有限，避免重复扫描、重复读取和长篇自我解释；先形成一个方向，再高质量落地。
- 不能为了省额度退回“只改 CSS”；方向错误比多一次短检查更浪费。
- 不读取或输出 .env.local、API Key、管理员令牌、认证信息、data/local-users 或 data/local-cache。
- 当前 Git 回滚基线是 b743c2c；不要 reset，不要覆盖 Git 历史，不要 commit/push/deploy。

现在开始。先读取指定文档并给出不超过 10 条的设计命题，然后立即实现旗舰路径第一检查点。
```

## 3. 第一检查点之后

先由用户在本地浏览器检查旗舰路径并直接批注。只有用户明确认可方向后，才在同一个新 Kimi 会话中继续迁移第二类核心任务和次级能力。

可使用下面的续接消息：

```text
旗舰路径的视觉和交互方向已确认。现在进入功能迁移阶段：读取旧 App.jsx 和相关组件，只提取状态、事件、API 与边缘功能，不继承旧 DOM、CSS 或信息顺序。

把另一类核心任务、真实相机/相册、语音与文本兜底、ROI、库存确认、加载/失败降级、重新规划和状态恢复接入新体验。随后把反馈、方案历史、先吃清单、冰箱整理、模拟补购、两轮救援和生活记录放入你认为最合理的位置。

现有后端不是限制。如果新设计需要新的字段、端点、流式状态、Agent 能力或固定演示数据，可以直接修改 `demo/server.mjs`、`demo/agent/`、Schema 与前后端适配，或者留下可由 Codex 直接实现的精确契约。新增内容必须服务核心故事，并具有失败兜底。

保持已经确认的新设计语言，不允许为了快速迁移旧功能而重新堆回旧卡片结构。完成后按实际改动运行一次必要的前后端目标检查与 npm run showcase:build，并报告仍需 Codex 审计或补齐的 API、状态和失败边界；不部署、不提交、不推送。
```
