# Demo 方案：活动海报视觉助手

更新日期：2026-05-23  
默认赛道：视觉搜索  
方案定位：现场可试玩的校园海报视觉理解 Agent

## 1. 一句话定位

拍一张校园活动海报，AI 自动读懂活动信息，并根据用户身份给出“要不要去、怎么去、怎么分享”的行动卡片。

## 2. 用户与场景

目标用户：

- 路过公告栏、活动摊位、社群转发海报的大学生。
- 想快速判断活动是否值得参加，但不想逐字阅读海报的人。
- 游园会现场的评委、游客和其他选手。

触发时刻：

- 用户看到一张信息密度很高的活动海报。
- 用户只愿意花 10 到 30 秒判断是否值得参加。

核心问题：

- 海报信息散、字多、重点不清。
- 不同用户关心点不同：有人关心技术含量，有人关心社交，有人关心福利、地点、时间冲突。
- 看懂之后还需要下一步行动：报名、收藏、发给同学、加到日程。

## 3. 体验闭环

主流程：

1. 用户打开 Web Demo。
2. 上传一张活动海报，或点击预置样例。
3. 选择自己的身份标签：技术向、社交向、竞赛向、薅福利、时间紧。
4. AI 输出活动摘要：
   - 活动名称
   - 时间地点
   - 报名方式
   - 关键信息
5. AI 输出个性化判断：
   - 推荐指数
   - 适合/不适合原因
   - 到场前要准备什么
6. 用户点击生成：
   - 日程提醒文案
   - 转发给朋友的话
   - 一张可分享卡片

30 秒演示路径：

1. 选预置海报。
2. 点身份标签“技术向”。
3. 展示结构化摘要和推荐指数。
4. 点“生成转发文案”。
5. 展示可复制结果和分享卡。

## 4. 页面结构

建议做成单页应用：

| 区域 | 功能 | 现场优先级 |
|---|---|---|
| 顶部状态栏 | 产品名、赛道、当前模式 | 中 |
| 海报输入区 | 上传图片、拍照、预置样例 | 高 |
| 身份标签区 | 技术向、社交向、竞赛向、薅福利、时间紧 | 高 |
| AI 理解结果 | 摘要、时间地点、报名方式、亮点 | 高 |
| 个性化建议 | 推荐指数、适合原因、准备事项 | 高 |
| 行动生成区 | 日程提醒、转发文案、分享卡 | 高 |
| 兜底状态 | 模型异常时展示缓存分析 | 高 |

## 5. 技术实现

最小可行架构：

```text
Web UI
  -> 图片上传/预置样例
  -> 后端 API
  -> 多模态模型识别海报
  -> 结构化 JSON
  -> 前端渲染摘要、推荐、分享卡
```

建议技术栈：

| 部分 | 建议 |
|---|---|
| 前端 | Vite + React，或纯 HTML/CSS/JS 快速原型 |
| 后端 | Node/Express 或 Python/FastAPI |
| 模型 | 可用多模态大模型，优先保证接口稳定 |
| 兜底 | 预置样例 JSON + 本地缓存结果 |
| 部署 | 本地启动 + 局域网访问，或静态页面 + 本地 API |

模型输出建议固定为 JSON：

```json
{
  "title": "活动名称",
  "time": "时间",
  "location": "地点",
  "signup": "报名方式",
  "highlights": ["亮点 1", "亮点 2"],
  "risks": ["注意事项"],
  "audienceFit": {
    "persona": "技术向",
    "score": 86,
    "reason": "推荐原因",
    "prep": ["需要准备的事项"]
  },
  "actions": {
    "calendarText": "日程提醒文案",
    "shareText": "转发文案",
    "questionSuggestions": ["可以追问的问题"]
  }
}
```

## 6. Prompt 草案

系统提示词：

```text
你是校园活动海报视觉理解助手。请从用户上传的活动海报中提取结构化信息，并根据用户身份给出是否值得参加的建议。
要求：
1. 优先提取活动名称、时间、地点、报名方式、主办方、面向人群。
2. 不确定的信息要标注为“未识别”，不要编造。
3. 输出必须是 JSON，不要输出 Markdown。
4. 建议要结合用户身份标签，给出具体行动，而不是泛泛评价。
```

用户提示词模板：

```text
用户身份标签：{persona}
请分析这张活动海报，输出活动摘要、推荐指数、适合原因、到场准备事项、日程提醒文案和转发给朋友的短文案。
```

## 7. 预置样例

至少准备 3 张图：

| 样例 | 用途 |
|---|---|
| 比赛/黑客松海报 | 贴合当前赛事，便于评委理解 |
| 学术讲座海报 | 展示信息提取和适合人群判断 |
| 社团/游园活动海报 | 展示传播性和普通用户价值 |

每张图都准备一份缓存 JSON，接口失败时直接展示。

## 8. 现场演示脚本

60 秒版：

```text
我们做的是一个校园海报视觉助手。大学里每天都有很多活动海报，但大家通常只扫一眼，很难马上判断值不值得去。

这里我直接上传一张活动海报，选择我的身份是技术向。AI 会先读出活动名称、时间、地点和报名方式，然后判断这个活动为什么适合我、需要提前准备什么。

最后它还能生成日程提醒和转发给同学的话。也就是说，它不是只告诉你“这是什么”，而是帮你从看到海报走到下一步行动。
```

3 分钟版结构：

1. 痛点：海报信息密度高，用户不愿细读。
2. 场景：路过公告栏或刷到活动图，只愿花 30 秒。
3. Demo：上传海报、选择身份、展示结果、生成行动。
4. AI：视觉理解 + OCR + 结构化提取 + 个性化推荐。
5. 价值：降低参与门槛，提升校园活动触达效率。
6. 兜底：预置样例和缓存结果保证现场稳定。

## 9. 评分点对齐

| 评分关注 | 对齐方式 |
|---|---|
| 用户价值 | 帮学生快速判断活动是否值得参加，并直接生成行动 |
| AI 能力与产品结合 | 多模态识别不是独立展示，而是驱动摘要、推荐和分享 |
| 体验完整性 | 上传/选择样例 -> 个性化分析 -> 行动卡片 -> 分享 |
| 创新性 | 从“识别海报”推进到“决策和行动” |
| 现场表现 | 可用真实海报试玩，也有预置样例和缓存兜底 |

## 10. 开发拆分

第一阶段：静态闭环

- 完成单页 UI。
- 放入 3 个预置样例和缓存 JSON。
- 做出身份标签切换和结果卡片。

第二阶段：模型接入

- 增加上传图片。
- 增加后端模型调用。
- 将模型输出约束为固定 JSON。

第三阶段：展示增强

- 生成分享卡。
- 增加复制按钮。
- 准备演示录屏。
- 调整移动端展示。

## 11. MVP 验收标准

- 不联网时，预置样例可以完整演示。
- 模型失败时，页面不会崩溃，会切换到缓存结果。
- 评委 30 秒内能看到上传/选择、AI 摘要、推荐理由和行动文案。
- 游客不用听长解释，也能自己点一次看懂。

