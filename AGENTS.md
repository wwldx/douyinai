# AGENTS.md

本文件是项目级协作规范与长期工作记忆。后续 Codex 或其他开发 Agent 在本仓库工作时，应先阅读本文件，再读取具体任务相关文档。

开发速度、验证分级、计划触发条件和权威文档治理统一遵守 `docs/strategy/开发节奏与验证分级标准-2026-07-15.md`。小改不得自动升级为全量回归、部署、报告或多文档同步。

## 1. 项目当前主线

当前主线是大区赛赛道四「视觉搜索」下的「冰箱晚餐 Agent」，不是「活动海报行动卡」。

核心定位：

> 刷到想吃的菜或打开冰箱时，拍一下/圈一下/说一句，AI 根据可见食材、用户状态和时间约束，判断今晚能不能照着做，并推荐最现实的一顿饭。

海报方向保留为备选和历史方案，不作为当前实现主线。若后续文档中同时出现海报方向和冰箱方向，以冰箱晚餐 Agent 为准。

大区赛赛道四信息已归档到 `docs/competition/大区赛宣讲会要点-2026-07-07.md`。后续路演和产品文案必须围绕官方赛道四评审锚点展开：真实场景成立、多模态必要性、抖音生态放大、AI 链路可落地、增长与 ROI。

大区赛按单人参赛模式推进。官方规则允许大区赛重新组队，队伍仍为 1-4 人且参赛者必须现场签到；用户已确认原队友均不参加重庆大区赛。后续开发、部署、评测、Skill、海报、路演和现场应急均不得依赖队友交付，任何范围设计都要以单人可完成、可维护为硬约束。

大区赛后续升级以 `docs/roadmap/大区赛升级总计划-2026-07-13.md` 为唯一总入口；历史 case 检索方案见 `docs/algorithm/历史Case检索与评测方案-2026-07-13.md`，公网发布见 `docs/deployment/公网部署与回滚计划-2026-07-13.md`，冰箱救援与其他扩展方向见 `docs/roadmap/冰箱管理与扩展方向评估-2026-07-13.md`。新想法必须先进入升级矩阵并明确评分价值、Solo 成本、验收条件和回滚方式，不能只在聊天中扩张范围，也不能再创建并列的当前计划。

## 2. 当前实现状态

当前 demo 已实现：

- `frontend/`：面向评委和现场演示的 Vite + React 展示端，首屏提供 Feed 和冰箱两个真实场景入口。Feed 路线按「菜图/语音 -> 冰箱 -> 库存确认 -> 目标菜规划」执行；冰箱路线按「冰箱 -> 库存确认 -> 自由推荐/指定菜」执行；支持相机/相册分开输入、暂停帧手动矩形框选、一键示例、结果页显式反馈，以及饭后成品图生成可编辑生活记录草稿。
- `frontend/src/components/ImageFocusSelector.jsx`：U11 手动 ROI 组件；用户在暂停帧上拖出矩形并将裁剪区域重新交给目标菜视觉模型，支持选整张、取消和恢复原图。它不是自动目标检测或像素级分割。
- `frontend/src/components/SpeechInput.jsx`：目标菜真实语音输入，优先使用浏览器 Web Speech，失败时录音调用 `/api/transcribe-audio`，文本框始终作为可编辑兜底。
- `demo/index.html`：冰箱晚餐 Agent 单页界面。
- `demo/app.js`：前端交互、缓存样例、库存确认、等待状态、上次识别缓存、晚餐方案渲染。
- `demo/dev.html`：开发检验台，用于人工核对每一步链路结果。
- `demo/dev.js`：开发检验台交互，展示视觉识别原始 JSON、归一化库存、人工确认库存、晚餐规划输入与输出，并支持加载上次成功识别结果；可切换 `off`、`positive`、`contrast` 并预览历史 Case Top-K、分项分数和来源；“检查粗分区”可独立查看 `notes -> zone` 证据和移动规则原始 JSON；生活记录区可查看请求、结构化草稿、来源和耗时。
- `demo/dev.css`：开发检验台样式。
- `demo/styles.css`：页面视觉和响应式布局。
- `demo/server.mjs`：Node 服务；开发时提供 API，生产时同源承载 `frontend/dist`、示例资源和 API。
- `demo/agent/`：轻量 Agent 模块，包含视觉识别 Agent、晚餐规划 Agent、模型客户端和 JSON Schema。
- `demo/agent/ingredientSubstitutionEngine.mjs`：U15 确定性食材替代引擎；按指定食材与人工确认库存做白名单匹配，区分原料已有、可改做法和仍需补买，不调用大模型。主展示已移除单项选择与结果卡，该模块只留开发检验。
- `demo/agent/eatFirstEngine.mjs`：U16 确定性先吃清单引擎；只根据用户主动确认的已开封、标签日期临近或状态不确定进行分组，不从图片推断过期、新鲜度或可安全食用。
- `demo/agent/fridgeZoneEngine.mjs`：U17/U18 确定性粗分区与布局预览引擎；将视觉 `notes` 归一化到门架、上/中/下层、抽屉、冷冻区或未知，最多返回三条建议，并生成“现在/建议后”规则预览。
- `demo/agent/lifeLogDraftAgent.mjs`：U10 成品图生活记录 Agent；输入真实成品图和用户确认菜名，输出标题、封面文案、旁白、补拍镜头、标签和人工确认提醒，不执行真实发布。
- `demo/profile-store.js`：用户画像 MVP，优先通过本地服务写入用户文件夹，显式偏好、反馈事件和推断 traits 都按用户隔离。
- `demo/speech-input.js`：旧展示页和开发检验台的语音输入实现。
- `scripts/mac-speech-transcribe.swift`：macOS Speech Framework 转写辅助脚本，由本地 Node 服务调用；需要 macOS 语音识别权限和 `ffmpeg`。
- `data/demo-users/`：可提交的演示用户种子，包括新手赶时间、深夜写代码、健身轻食。
- `data/demo-cache/vision/`：可提交的视觉预分析缓存，覆盖 `assets/fridge-images`、`assets/imgs-ai`、目标菜和成品图演示图片；每次返回保留 `model`、`model-timeout-cache` 或 `model-error-cache` 来源。
- `data/eval/planning-cases.json`：19 例目标菜/晚餐规划评测；与 11 例视觉缓存样例组成 30 例可重复评测集。
- `data/eval/ingredient-substitution-cases.json`：12 例食材替代规则回归，覆盖精确命中、保守改版、拒绝不可靠替代和安全提示。
- `data/eval/eat-first-cases.json`：10 例先吃清单规则回归，覆盖本餐优先、近两餐、待确认、不提醒和排序稳定性。
- `data/eval/fridge-zone-cases.json`：18 例粗分区与整理规则回归，覆盖六类分区、歧义拒绝、同区前移、跨区确认门槛、豆腐反例、三条上限和布局前后变化。
- `scripts/run-life-log-cache-eval.mjs`：U10 生活记录演示草稿回归，检查 Schema 形状、补拍措辞、人工确认提醒和禁止的发布/效果声称。
- `data/case-memory/`：24 条独立人工参考 Case 和 8 条检索标签；不得与 19 条规划 holdout 混用，也不得称为真实用户数据。
- `data/local-users/`：本机运行时用户记忆和每个用户的冰箱识别缓存，已在 `.gitignore` 中排除。
- `data/local-cache/`：本机运行时模型成功返回后的视觉缓存，已在 `.gitignore` 中排除。
- `assets/fridge-images/`：演示冰箱图片。
- `docs/demo/测试演示操作手册.md`：本地启动、页面入口、测试流程、常见问题和 GitHub 上传步骤。
- `docs/demo/双路线演示操作文档.md`：解释「识别冰箱」「生成晚餐方案」「判断复刻路线」三类按钮的关系、推荐演示顺序和现场讲法。
- `docs/competition/大区赛宣讲会要点-2026-07-07.md`：大区赛赛程、赛道四解读、评审权重、差旅、场次选择和 FAQ 的本地摘要。
- `docs/strategy/AI_Agent情景化Demo展示工作流.md`：可长期复用的 Agent 项目展示方法论，沉淀「情景视频 -> 用户端 Demo -> 开发检验台」三层讲解、视频脚本、讲稿模板、可信证明和跨项目复用流程。
- `总结经验/`：面向后续比赛和新 Agent 项目的可迁移经验资料包，包含启动提示词、选题收敛、MVP 架构、开发检验台、模型兜底、用户话术、现场作战与赛后复盘模板；新项目可整体复制并优先交给 Agent 阅读。
- `/api/analyze-fridge`：视觉识别 Agent，输入冰箱图片 data URL，输出结构化库存 JSON。
- `/api/analyze-target-dish`：目标菜图识别 Agent，输入想复刻的菜图，输出菜名猜测、关键材料、工具、耗时、难度和不确定提醒。
- `/api/plan-dinner`：晚餐规划 Agent，输入确认后的库存和用户上下文，输出结构化晚餐方案 JSON。
- `/api/plan-target-dish`：目标菜复刻规划 Agent，输入确认库存、目标菜文字和用户画像，输出尽量复刻路线、缺料、难点提醒和抖音商城/本地生活模拟卡。
- `/api/generate-life-log`：成品图生活记录 Agent，输入成品图 data URL 和用户确认菜名，输出可编辑标题、封面、旁白、补拍建议和标签；接口保留模型/缓存来源，不自动发布。
- `/api/transcribe-audio`：语音兜底接口，输入浏览器录音 data URL，转成临时 wav 后尝试音频转写；临时音频处理完成即删除。U24 已加入腾讯云一句话识别 TC3 服务端 provider，仍保留浏览器 Web Speech、显式启用的 OpenAI Audio API、macOS Speech 和手动文本降级。腾讯云公网链路和 005 iPhone 麦克风允许/拒绝兜底已有历史证据；012 的手机复验属于发布矩阵，不重新打开语音功能开发。
- `/api/case-retrieval/preview`：只运行本地确定性结构化检索，不调用大模型；返回 Top-K、正反例角色、分项相似度和共同证据。
- `/api/ingredient-substitution`：只运行本地确定性白名单规则，不调用大模型；输入画面关键食材和用户确认库存，输出 `exact_match`、`adapt_recipe` 或 `shop_needed` 以及可审计证据。
- `/api/eat-first`：只运行本地确定性状态规则，不调用大模型；输入用户确认后的 `itemStates`，输出本餐优先、近两餐、待确认和 Planner 优先食材。
- `/api/fridge-organization`：只运行本地确定性粗分区和整理规则，不调用大模型；输入确认库存、可选状态与包装确认，输出分区证据、最多三条建议和“现在/建议后”布局预览。
- 服务端本地用户文件夹：当前主路径使用 `data/local-users/<userId>/profile.json` 和 `vision-cache.json` 保存用户画像、反馈事件和每个用户的上次冰箱识别缓存。
- 浏览器本地缓存：这里指浏览器站点存储 `localStorage`，不是项目目录里的长期文件；它仅保留当前选中用户，并在本地服务不可用时兜底，不作为主存储；缓存不保存 API Key。
- `.env.local`：本地 API Key 和模型配置，仅本机使用，不提交。
- `.env.example`：给队友参考的配置模板，可以提交。
- `scripts/push-to-github.sh`：一键检查、提交、推送脚本。
- `scripts/run-evals.mjs`：评测数据校验和真实 API 评测脚本，输出 Schema、动作合理性、安全语义、来源、延迟、失败分类和 provider token usage；支持 `--timeout-ms`。
- `npm run eval:model-smoke`：固定 8 例混合真实模型小集，覆盖 4 例视觉与 4 例规划；`--require-model-source` 会拒绝缓存来源。真实视觉评测服务必须使用 `DEMO_VISION_CACHE_ENABLED=false` 启动。
- `scripts/run-retrieval-evals.mjs`：历史 Case 检索数据校验与 Recall@4/MRR@4 回归；运行命令为 `npm run eval:retrieval`。
- `scripts/package-cloudbase.sh`：生成 CloudBase Run 安全部署包；运行 `npm run cloudbase:package`，产物不会包含 `.env.local`、Git、本地用户数据或 `node_modules`。
- `Dockerfile`：生产单容器构建，包含 React H5、Node API 和 `ffmpeg`。
- `submission/skill/fridge-dinner-visual-search/`：官方 Skill 源文件；`npm run skill:package` 生成 `.skill` 上传包。

模型接口配置当前支持 OpenAI 兼容 Responses API。当前中转站推荐配置：

```env
OPENAI_API_KEY=你的中转站 API Key
OPENAI_BASE_URL=https://right.codes/codex/v1
MODEL_PROVIDER=rightcode_responses_stream
RIGHTCODE_CHAT_BASE_URL=https://www.right.codes/draw
RIGHTCODE_CHAT_MODEL=gemini-3.1-pro
OPENAI_MODEL=gpt-5.6-terra
VISION_MODEL=gpt-5.6-terra
PLANNING_MODEL=gpt-5.6-terra
LIFE_LOG_MODEL=gpt-5.6-terra
DISABLE_RESPONSE_STORAGE=true
CASE_RETRIEVAL_MODE=off
CASE_RETRIEVAL_LIMIT=4
API_RATE_LIMIT_WINDOW_MS=60000
API_RATE_LIMIT_MAX=80
MODEL_RATE_LIMIT_MAX=30
TRUST_PROXY=false
# DATA_DIR=/data/fridge-agent
```

说明：

- `MODEL_PROVIDER=rightcode_responses_stream` 时，demo 会走 Right Code Codex `/responses`，并强制使用 `stream:true` 解析 SSE。
- `VISION_MODEL`、`PLANNING_MODEL`、`LIFE_LOG_MODEL` 可按任务独立路由；任何一项未填写时回退到 `OPENAI_MODEL`。公网 012 已统一使用 `gpt-5.6-terra`；011 是首选回滚版本，010 是次级回滚点。
- 当前账号的 `/draw/v1/models` 只列出图像生成模型，不适合本项目的图片理解；`/draw/v1/chat/completions` 保留为备用 provider。
- 2026-07-18 已完成当前 `gpt-5.6-terra` 固定 30 例真实模型基线：30/30，全部 `source=model`；平均 18.33 秒、P95 28.03 秒、总 114,467 tokens，Schema、规划动作和禁用语义门禁均为 100%。视觉输入采用展示端等效的最长边 1400px JPEG；该结果只代表固定回归集，不外推为真实用户准确率。原 8 例混合小集保留为历史证据。
- 同一 4 例视觉小集中，`gpt-5.6-sol` 与 `gpt-5.6-terra` 首轮均通过 3/4，平均延迟分别约 63.2 秒和 20.3 秒；`gpt-5.6-luna` 0/4，不采用。重复菜图识别仍存在细粒度菜名波动，因此用户确认菜名是硬步骤。
- 2026-07-18 的 009 公网记录与 Right Code 账单共同确认：`gpt-5.6-sol` 两次在应用 50 秒门限结束并显示 0 token，另一次在约 47 秒完成并正常计 token；0 token 是请求超时前没有完整响应，不是 token 解析错误。定向对照中，`gpt-5.6-terra`、`gpt-5.5-openai-compact`、`gpt-5.5` 均在约 32-37 秒完成目标菜单例，但后两者分别出现外卖倾向和调味包误判。补充业务门禁后，`terra` 的本地黄焖鸡目标菜单例 34.9 秒、番茄鸡蛋自由晚餐 16.1 秒完成；010 公网固定黄焖鸡请求 31.0 秒完成并正常记录 6,447 tokens。以上只支持本次定向换路由，不外推为完整质量基线。
- 真实菜图测试暴露了素材真值问题：旧“黄焖鸡”文件实际更像干锅鸡块，多个模型给出干锅类判断。2026-07-18 已替换为食材特征明确的黄焖鸡示例图；菜名用户确认仍是硬步骤，不得把文件名当作模型真值。
- 历史 Case 检索公网保持关闭：当前 `terra` 完整 19 例中 `off=19/19`、`positive=19/19`、`contrast=18/19`；后两组总 token 分别增加 19.2%/24.1%，未证明质量增益且 contrast 引入一例动作退化。
- 服务端已加入 12MB 请求体上限、模型/普通 API 分组限流、生产错误脱敏、安全响应头、request ID 和 `Server-Timing`。部署在可信反向代理后才设置 `TRUST_PROXY=true`。
- 公网平台文件系统只读或临时时，将 `DATA_DIR` 指向持久化可写挂载；它只保存 `local-users`，不替换镜像内只读的演示种子、视觉缓存和 Case 库。
- `npm run smoke -- http://localhost:4173` 可复核页面、健康状态、安全头、Case Top-K 和错误脱敏；公网部署后将参数替换为 HTTPS 地址。
- 公网平台已选定腾讯云 CloudBase Run，上海环境 ID 为 `fridge-dinner-agent-d7bpec7d4611`。CloudBase Run 没有供本项目使用的公网 IP/SSH 部署入口，使用系统 HTTPS 域名、代码包和版本流量管理。
- CloudBase 容器文件系统不持久化；在用户记忆迁移到云数据库前，版本最大实例数保持 `1`，比赛前将最小实例数从 `0` 调整为 `1`。

## 3. 运行与配置

本地启动：

```bash
cd "/Users/wwl_mac/Desktop/2026春季第四学期/2026抖音ai创变者计划"
npm run dev
```

默认地址：

```text
http://localhost:4173/
```

开发检验台：

```text
http://localhost:4173/dev.html
```

如果使用了 `PORT=4174 npm run dev`，则对应为：

```text
http://localhost:4174/
http://localhost:4174/dev.html
```

健康检查：

```text
http://localhost:4173/api/health
```

若端口被占用，说明已有本地服务在运行。可以直接使用当前服务，或先停止旧进程后重启：

```bash
pgrep -af "node demo/server.mjs"
kill <PID>
npm run dev
```

也可以换端口：

```bash
PORT=4174 npm run dev
```

展示端本地启动需要两个终端：

```bash
npm run dev
```

```bash
npm run showcase
```

然后打开：

```text
http://localhost:5173/
```

`frontend/vite.config.js` 默认把 `/api/*`、`/sliced/*` 和 `/demo-assets/*` 代理到 `http://localhost:4173`。如果后端端口变化，启动展示端时设置 `BACKEND_ORIGIN`，例如 `BACKEND_ORIGIN=http://localhost:4174 npm run showcase`。

生产模式只需要一个端口：

```bash
npm run build
PORT=4173 npm start
```

此时 `http://localhost:4173/` 是 React 展示端，`/dev.html` 保留为开发检验台。公网部署优先使用仓库根目录 `Dockerfile`，并在平台 Secret 中配置 API Key。

注意：

- `.env.local` 修改后必须重启服务才会生效。
- 前端不能直接读取或暴露 API Key。
- API Key 只允许存在于 `.env.local`、本地 shell 环境或用户自己的安全配置中。
- 不要提交 `.env.local`、真实 API Key、真实用户隐私数据或真实照片样本。

## 3.1 展示界面与开发检验台分工

当前项目采用三类前端入口：

- `frontend/`：当前主展示端，给评委和现场观众看，隐藏后台画像、模型细节和 JSON，只保留双场景入口和用户可理解的决策步骤。
- `demo/index.html`：旧展示页/本地兜底页，可用于对比和保留完整按钮链路。
- `demo/dev.html`：面向开发调试和人工验收，必须把每一步的输入、输出和错误摊开显示。

调试视觉识别或晚餐规划时，优先打开开发检验台，而不是只看展示界面。开发检验台应至少显示：

1. 服务健康状态：provider、model、是否读取到 API Key。
2. 上传原图预览。
3. `/api/analyze-fridge` 原始返回 JSON。
4. 归一化后的库存列表和人工勾选状态。
5. `/api/plan-dinner` 请求输入，尤其是人工确认后的 `inventory`。
6. `/api/plan-dinner` 原始返回 JSON。

展示界面可以保留缓存样例作为现场兜底，但不能在模型识别失败时把缓存食材伪装成模型结果。识别失败时必须明确提示“未使用缓存结果”，方便判断真实链路是否正常。

展示端用户可见信息边界：

- 可以展示：冰箱照片上传、食材确认、餐次、用户愿意花多久、用户自己选择的模式、是否有目标菜、最终做法、缺什么、必要难点提醒、少量补买建议。
- 餐次可以根据当前时间预选，但必须允许用户手动改；不能把时间推断说成确定事实。
- 可支配时间、精力状态、想不想复刻目标菜必须由用户确认，不能只靠 AI 猜。
- 不展示：`用户画像 MVP`、内部 traits、长期记忆字段、模型名称、provider、原始 JSON、confidence、schema、API 错误栈、后台缓存路径。
- 不展示“AI 判断你今晚很累”这类不可证实结论；“今天很累 / 时间忙 / 想尝试新菜”只能作为用户显式选择。
- 如果用户选择自己做饭路线，不要突出外卖兜底；外卖只留在开发页或极端失败/安全场景里。
- 语音是目标菜文本框旁的可选快捷入口，不得阻断主流程；浏览器识别或服务端 ASR 失败时立即回到文本输入。
- 展示端结果页的“重新规划”应回到库存确认阶段，保留已识别冰箱、确认库存、餐次和时间；“换一种开始方式”或点击品牌返回场景入口并清空当前流程。
- 结果页要体现抖音生态闭环：缺关键食材时展示“加入购物车，去抖音商城”模拟按钮；饭后可拍摄/上传成品图，生成可编辑标题、封面文案、旁白、补拍建议和标签。当前不做真实下单或发布，保存按钮只能确认当前演示草稿。

等待体验要求：

- 视觉识别和晚餐规划等待期间必须显示已用秒数。
- 等待状态要提示当前阶段，例如发送图片、等待视觉模型、模型仍在识别。
- 超过 45 秒的视觉识别要提示可继续等待或加载上次识别结果。
- 成功识别后自动缓存本次结构化结果，展示页和开发检验台都应能一键加载。

## 4. GitHub 上传规则

推荐使用脚本：

```bash
npm run push -- "提交信息"
```

或：

```bash
./scripts/push-to-github.sh "提交信息"
```

脚本会执行：

1. 进入项目根目录。
2. 运行 `npm run check`。
3. `git add -A`。
4. 有改动时自动 commit。
5. 推送到当前分支的 `origin`。

`.env.local` 已被 `.gitignore` 忽略，脚本不会上传本地密钥。

## 5. 产品与 Demo 原则

实现优先级：

1. 现场稳定性优先于架构完整性。
2. 30 秒内让评委和游客理解价值。
3. 先完成一个具体场景闭环，不做泛 AI 助手。
4. 模型能力必须有缓存兜底。
5. 视觉识别结果必须驱动行动，不只展示“识别到了什么”。
6. 用户确认库存是核心步骤，用来处理多模态识别不确定性。
7. 不确定食材必须标注，不允许硬编。
8. 食材新鲜度、过期、剩菜安全、肉类状态必须交给用户确认。
9. 冰箱整理最多识别门架、层架、抽屉等粗粒度分区；日期 OCR 只能作为待确认候选，不能生成 `safeToEat` 结论。
10. 新手用户不推荐高风险烹饪动作，例如油炸、复杂刀工、整鱼整鸡处理。
11. 商业建议要克制，只服务当下决策，例如少量补货、外卖兜底、厨具升级建议。
12. 对外展示优先采用三层结构：情景视频解释为什么值得用，用户端 Demo 证明能真实操作，开发检验台证明不是写死页面。

比赛阶段的数据与隐私优先级：

- 当前是面向评委和小范围熟人测试的创意比赛 Demo，默认优先投入比赛呈现、链路稳定、问题定位和可观测性，不得因假设性的产品级隐私需求阻塞功能开发、验证或部署。
- `agent_runs` 可以保留排障与演示复盘所需的白名单结构化输入和输出，包括确认库存、目标菜文本、用户显式偏好、检索证据、规划结果、来源、耗时和 token；无需主动把这些字段继续泛化脱敏。
- 不主动建设复杂同意流、字段级加密、自动匿名化、隐私后台或合规体系。只有用户明确要求、测试范围升级为真实公开产品，或出现具体合规要求时，才单独立项评估。
- 仍不得记录或提交 API Key、管理员令牌、认证请求头等凭据，也不持久化原始照片和原始音频；这是防止服务故障、密钥滥用和存储失控的最低工程边界，不作为扩张隐私工作的理由。

## 6. 模型与 Agent 架构

当前采用轻量两段式 Agent，而不是重框架：

```text
视觉识别 Agent
  -> 输出 items / uncertainItems / warnings
  -> 用户确认库存
晚餐规划 Agent
  -> 输出 decision / baseMeal / stretchMeal / shoppingUpgrade / fallback / commerceSuggestion
```

暂不引入 LangChain、LangGraph、Dify 等重框架。原因：

- 比赛 demo 更需要稳定、可解释、容易兜底。
- 四个端点已经能清楚表达目标菜视觉、冰箱视觉、自由晚餐规划和目标菜规划的分工。
- 后续如果需要迁移到 OpenAI Agents SDK、LangGraph、Dify 或其他 provider，只需替换服务端 provider，前端 JSON 合约尽量保持不变。

当前架构决策：

- 继续使用原生 Node.js + 轻量 Agent 函数 + 结构化 JSON 合约。
- 大区赛故事链：在抖音刷到想吃的菜，暂停/圈选/上传目标菜图，再拍冰箱或说出约束，AI 判断今晚能不能照着做。这条链路比普通聊天机器人更贴合赛道四视觉搜索，因为它连接内容种草、视觉理解、语音/文本意图、现实库存、交易补齐和后续生活记录。
- 暂不为了“看起来像 Agent 框架”而引入 LangChain/LangGraph/Dify。
- 当出现多轮追问、工具调用、状态回滚、分支流程和复杂记忆写入时，再评估 LangGraph 或 OpenAI Agents SDK。
- 已实现第一版 `profileStore`、反馈事件、画像 traits 和规划 Agent 输入；后续重点是优化 traits 规则和“本次参考依据”的展示质量。
- 用户画像和冰箱识别缓存必须按用户隔离，优先写入 `data/local-users/<userId>/`；不要只依赖浏览器 `localStorage`。
- 演示视觉缓存必须遵守“模型优先、缓存兜底”：前端上传文件名，后端先调用模型；如果超过 `DEMO_VISION_CACHE_FALLBACK_MS`，默认 4500ms，且命中 `data/demo-cache/vision/`，才返回缓存。不能因为文件名命中就秒回，避免现场显得假。
- 视觉接口返回要保留 `source`：`model`、`model-timeout-cache` 或 `model-error-cache`，开发台可见；展示端不展示这些内部字段。
- 展示端面向用户时少说“复刻”“关键缺口”等技术感词汇，优先表达为“照着做”“看看家里够不够做”“还差哪些材料”“顺手补点材料”；目标菜规划不展示“可做指数”或分数，避免解释算法成本。
- 当前双入口采用两个明确上传区：冰箱照片和目标菜图分开上传。不要在当前主流程里做多图自动分类，避免现场把菜图误当冰箱、冰箱图误当目标菜。
- Feed 菜图识别前可由用户手动拖出矩形框选画面重点，浏览器只裁剪并重传选区；这不是模型自动检测、跟踪或像素级分割。菜名确认后直接进入拍冰箱，不再展示 `likelyIngredients` 单项选择；整道菜材料与确认库存由 Planner 统一处理。
- 库存确认页的“有食材想优先处理”是可选入口；只有用户显式选择已开封、确认标签日期临近或状态不确定后才调用 `/api/eat-first`。优先项会进入 Planner 约束并在结果页解释，规则失败时必须继续原规划；任何先吃结果都不代表食材未过期或可以安全食用。
- `/api/fridge-organization` 可以复用视觉 `notes`，但只允许 `door/top/middle/bottom/crisper/freezer/unknown` 粗分区。描述跨多个区域时必须返回 `unknown`；原分区前移只依赖用户主动标记，跨分区移动必须满足原蛋盒、已密封防漏生鲜或未切果蔬等显式确认条件。
- 结果页的“现在/建议后”只是规则预览：只应用已满足前提的建议，`confirm_first` 保持原位；不得声称用户已经整理完成，也不得把位置变化解释成食材安全状态变化。
- Feed 路线上传目标菜图后，必须先识别菜名并写入目标菜文本框，让用户手动修改或补充约束，再进入冰箱步骤；冰箱路线选择“我有想吃的”后也遵守相同确认规则。不能上传菜图后直接生成规划。
- 语音输入已经进入 React 展示端，但仅作为可选输入：浏览器 Web Speech 优先，服务端录音转写兜底，手动文本是最终稳定路径。部署必须使用 HTTPS 才能稳定申请麦克风权限。
- 30 例评测集是提示词、模型和 Schema 改动的回归基线；不得用缓存命中结果冒充真实模型评测成绩。
- 结构化历史 Case V1 已实现，但当前只有 24 条参考 Case 和 8 条人工检索标签；`Recall@4=100%`、`MRR@4=0.938` 只能描述该小型标签集上的检索结果，不能声称规划质量或线上指标提升。
- Planner 中的 `retrievedCases` 是可选参考：当前人工确认库存、目标菜文字、时间和用户约束优先；检索关闭或失败时必须回到 V0。

模型输出要求：

- 必须使用结构化 JSON。
- 不依赖自由文本解析。
- schema 改动时必须同步前端渲染和文档。
- 接口失败时保留缓存方案，页面不能空白。

## 7. 用户画像与长期记忆原则

用户画像是未来重点，但不要做成一坨不可解释的长期记忆。

核心公式：

```text
用户画像 = 显式偏好 + 行为事实 + 场景上下文 + 推断标签 + 反馈修正
```

分层原则：

| 层级 | 保存周期 | 内容 | 用途 |
|---|---:|---|---|
| 会话记忆 | 几小时到 1 天 | 本次对话、刚上传的冰箱图、刚确认的库存 | 连续追问 |
| 短期行为记忆 | 7 到 14 天 | 最近吃了什么、最近接受/拒绝什么 | 避免重复、识别近期状态 |
| 长期偏好记忆 | 1 到 6 个月 | 忌口、厨艺、常用锅具、口味、预算 | 个性化基础 |
| 推断画像 | 14 到 30 天滚动 | 深夜用餐倾向、低洗锅倾向、热食偏好 | 场景推荐 |
| 安全/健康记忆 | 长期，但强用户确认 | 过敏、疾病、医生建议 | 高风险约束 |

推断标签必须包含：

- `key`
- `label`
- `confidence`
- `evidence`
- `updatedAt`
- `expiresAt` 或其他有效期设计

重要边界：

- 行为事实不等于人格标签。
- 不能因为用户凌晨多次使用就直接说“你是夜猫子”。
- 可以中性表达为“深夜用餐请求较多”或“深夜热食接受率较高”。
- 不做价值判断，不使用“作息不健康”“自控力差”等标签。
- 高风险健康信息不能自动推断，必须由用户明确提供。
- 用户应能查看、修改、删除画像或关闭相关推断。

深夜场景推荐原则：

- 深夜优先推荐热、软、少油、好消化、少清洗负担的方案。
- 深夜“抗饿”不等于吃很撑，应使用适量主食 + 少量蛋白质。
- 可以推荐鸡蛋、豆腐、牛奶、酸奶、面、粥、米饭、土豆等组合。
- 避免重油、重辣、高糖饮料、复杂炒菜和长时间清洗。
- 如果明早有课或工作，应降低烹饪时长和洗锅成本。

用户画像分析建议先用规则实现，再用 LLM 做自然语言总结：

1. 记录行为事件。
2. 计算结构化特征。
3. 用规则生成推断标签。
4. 将显式偏好、特征和推断标签传给晚餐规划 Agent。
5. 推荐解释中说明本次参考了哪些依据。

## 8. MVP 暂不做事项

当前 MVP 暂不做：

- 长期营养健康诊断。
- 精准热量估算。
- 医疗建议或疾病饮食建议。
- 自动判断食材过期、变质或肉类安全。
- 真实外卖平台下单。
- 真实生鲜电商购买。
- 复杂家庭多人偏好协调。
- 账号系统和云端数据库。
- 大规模推荐系统或完整画像后台。

这些内容可以作为商业延展或后续路线图，但不要塞进当前 demo 主流程。

## 9. 未来规划队列

优先级较高：

- 按 `docs/roadmap/大区赛升级总计划-2026-07-13.md` 推进，Solo 同时只允许一个 `In progress` 升级。
- 腾讯云 CloudBase 当前公网版本为 012，环境 ID 为 `fridge-dinner-agent-d7bpec7d4611`，默认 HTTPS 域名为 `https://fridge-dinner-agent-281751-9-1304313771.sh.run.tcloudbase.com`；第三轮新前端、三任务 `terra`、严格公网冒烟、固定目标菜规划和 `agent_runs` 持久化已定向通过。012 异常时首选回滚 011，010 是次级回滚点。
- 012 第一、二轮视觉实现已保留为 Git 回滚点，但用户确认其仍属于旧结构上的换肤，不作为最终设计。第三轮使用全新 Kimi 3 会话，按 `docs/frontend/012第三轮真正前端重构设计简报-2026-07-20.md` 从产品任务重新设计；现有两类业务任务、API 和后端能力是可复用基线，不冻结颜色、页面数量、入口形态、信息顺序、组件或后端上限。Kimi 可提出或实现能增强比赛呈现的新前后端能力，Codex 负责后续契约审计、补齐与定向验证。
- 手机直接拍菜/拍冰箱、五类结果反馈、反馈约束重规划、阶段 trace 和公网前限流已经完成本地验收；剩余 HTTPS 真机和外网压力验证。
- 结构化历史 Case V1、正反例选择、开发台预览和当前 `terra` 完整 19 例消融均已完成；结果不支持进入主演示，公网保持 `off`，不继续扩展 embedding。
- U16“用户确认状态后的先吃清单”已完成本地端到端验收：规则回归 10/10、发布冒烟、真实规划采用优先食材、桌面与 390px 交互通过。历史单例使用 sol，当前发布基线统一为 terra；不得宣称自动判断过期、腐败或可安全食用。
- U17“粗粒度分区与最多三条移动建议”已完成本地端到端验收：18/18 规则回归、发布冒烟、真实模型结果页、开发台、桌面与 390px 交互通过；不做精确位置、三维重排或食品安全结论。
- U18 已收敛并完成“现在/建议后”确定性布局预览：只把满足前提的 `move_zone` 和 `same_zone_front` 应用到预览，未确认项保持原位；该预览不是二次照片验收，也不代表用户已执行。
- U10“成品图生活记录草稿”已完成本地端到端和真实模型定向验收：terra、`source=model`、约 26.65 秒、3,228 tokens，草稿可编辑且不自动发布。该证据只有一个固定成品图样例，不能外推为真实用户照片泛化能力。
- U15 白名单替代规则回归为 12/12，但“画面关键食材选择 -> 附加结果卡”已于 2026-07-16 从主展示移除：该步骤与后续拍冰箱重复且没有改变主规划。规则与接口仅留开发检验，不能再作为用户可见亮点讲述。
- U11“暂停帧手动框选”已完成本地端到端验收：拖动矩形、裁剪重识别、整图恢复、桌面与 390px 交互均通过；只称手动 ROI，不声称自动定位或分割。
- U22 分层模型路由与 token 采集已完成；010 公网严格健康检查已确认三任务均为 `gpt-5.6-terra`。固定规划请求 31.0 秒完成并记录 6,447 tokens；演示缓存图因 4.5 秒兜底策略可能先返回 `model-timeout-cache`，两种来源不得混写。
- OneReason 的预训练、结构化 CoT SFT、分域 GRPO、RFT/MOPD 属于大团队训练路线，本次 Solo 项目明确不复现、不包装；不得虚构 teacher、训练指标或蒸馏成果。
- 等待体验已加入阶段提示和计时；75 秒字幕/旁白备用视频与静音字幕版已生成，后续只根据真机或现场明确问题修正。
- 最近一次视觉识别结果已加入按用户隔离的本地文件缓存；主展示端也已加入固定示例冰箱和示例菜图入口。
- 准备真实冰箱样例图，继续测试 `/api/analyze-fridge` 的识别稳定性。
- 011 本地浏览器验收为 47/47，覆盖桌面与 390px、固定示例、局部补拍、补购重规划、方案历史、两轮救援、生活记录、开发台和运维台；这是本地自动化证据，不替代公网或 iPhone。
- U20 未知步骤真实模型样例已由 terra 在约 24.50 秒完成并记录 2,809 tokens；当用户无法说明当前阶段时，确定性后处理强制 `needsConfirmation=true` 和追问。该结论当前只有一个固定样例。
- 推荐反馈按钮已加入：`想吃`、`太麻烦`、`不够抗饿`、`不想洗锅`、`换清淡点`、`今天就想外卖`。
- 第一版 `profileStore` 已加入，优先用 `data/local-users/<userId>/profile.json` 保存显式偏好、事件和推断标签。
- 第一版画像分析函数已加入，生成 `lateNightWarmMealPattern`、`onePotPreference`、`quickMealPreference` 等可解释标签。
- 画像 traits 已传入 `/api/plan-dinner`，让晚餐规划 Agent 结合长期偏好生成方案。
- 页面增加“本次推荐参考了什么”区域，例如深夜时段、新手厨艺、不爱洗锅、最近外卖偏多。
- 目标菜文字输入已加入：用户输入“我今晚想吃番茄牛腩”，系统结合冰箱库存和画像判断复刻路线。
- 抖音商城/本地生活模拟卡已加入目标菜结果，商业建议只在缺料、工具缺口或兜底场景出现。
- 展示端结果页已加入抖音商城模拟购物车按钮和饭后生活记录入口，形成“内容种草 -> 冰箱判断 -> 补齐材料 -> 做完记录”的闭环。

优先级中等：

- 第二张目标菜图片已支持：先识别目标菜名和关键材料，再允许用户手动确认菜名。
- 多图自动分类暂不进主流程，只作为后续规划：一次上传多张图后由 AI 判断哪张是冰箱、哪张是目标菜。
- U24 已完成代码、离线回归、腾讯一句话识别公网端点和 005 iPhone 麦克风允许/拒绝兜底验收；10 条 macOS 合成语音 10/10 只证明合成链路。U24 为 Verified，012 的手机复验归入发布矩阵，不重新打开功能开发。
- CloudBase 012 部署包为 `dist/deployment/fridge-dinner-agent-cloudbase-012.zip`，SHA-256 为 `80cb1c97523d367e67e31f419f340cd72731d4bee22b5d009c93f44953131066`；压缩完整性和敏感文件门禁已通过，已部署并完成严格公网冒烟与单例真实规划。011 保留为首选回滚点。
- `.env.local` 已配置任务级模型路由；接口可审计 provider token usage，完整 30 例与 19 例 Case 消融均已完成。012 的三任务 `terra` 路由已由严格公网 `/api/health` 门禁确认生效。
- U23 抖音小程序适配已纳入候选但不进入关键路径。CloudBase 原生小程序接入主要是微信 `wx.cloud`；抖音小程序需要独立字节小程序工程、`tt.*` API、AppID、备案 HTTPS 域名和提审，现有 React H5 不能原样复用。当前投入优先给 012 真机、回滚准备和提交材料；完成后再按评分收益重评。
- 生活记录 Agent 已有一个 terra 真实模型固定样例；主演示仍保留明确区分来源的固定成品图缓存，所有草稿必须人工确认。
- 增加“我只有 15 分钟”“我不想洗锅”等追问入口。
- 增加更多缓存样例，覆盖早餐/夜宵/运动后/考试周等场景。
- 优化上传图片压缩，降低 API 请求体大小。
- 增加模型错误提示，区分无 Key、网络失败、schema 失败、图片过大。
- 补充队友运行说明和现场演示检查清单。

## 10. 文档更新规则

每次讨论产生以下内容时，必须更新项目文档：

- 产品主线变化。
- MVP 范围变化。
- 未来实现计划。
- 重要技术决策。
- 模型/provider 配置变化。
- 用户画像或长期记忆策略变化。
- 不做事项变化。
- 现场演示流程变化。
- GitHub 上传、运行、部署方式变化。

更新优先级：

1. 先更新 `AGENTS.md`，记录长期约束和决策。
2. 若影响项目总览，同步更新 `docs/strategy/项目工作台.md`。
3. 若影响演示流程，同步更新 `docs/demo/Demo方案.md`。
4. 若影响比赛提交，同步更新 `docs/submission/作品说明文档.md`、`docs/submission/路演讲稿.md`、`docs/submission/海报文案.md`。
5. 原始长对话可以保存为独立记录，例如 `docs/archive/对话记录-用户画像.md`；`AGENTS.md` 只保存提炼后的结论。

## 11. 给后续 Agent 的工作要求

在本仓库工作时：

- 先读 `AGENTS.md`。
- 再读与任务直接相关的文档，例如 `docs/demo/Demo方案.md`、`docs/fridge-agent/MVP规划与执行清单.md`、`docs/archive/对话记录-用户画像.md`。
- 若任务涉及路演、视频、讲稿、作品集或跨项目展示方法，先读 `docs/strategy/AI_Agent情景化Demo展示工作流.md`，并沿用「先讲人和场景，再讲用户端，最后讲开发检验台」的顺序。
- 若任务涉及新增功能、算法增强、界面重构、部署或排期，先读 `docs/roadmap/大区赛升级总计划-2026-07-13.md`，再按需读取其中指向的算法或部署文档；先更新矩阵状态，再开始实现。
- 若任务涉及启动新 Agent 项目、参加下一次比赛或复盘迁移经验，先读 `总结经验/README.md` 和 `总结经验/00_给未来Agent的启动提示词.md`，再按其中索引加载相关方法文档。
- 不要把历史备选方案误当成当前主线。
- 不要提交 `.env.local` 或任何密钥。
- 修改模型请求 schema 时，必须同步前端、文档和兜底逻辑。
- 做前端改动后，尽量用浏览器检查 `http://localhost:4173/`。
- 长期规划、产品原则和未来改动不能只留在聊天里，要写回项目文档。
- 发现用户手动新增的文档或代码时，默认视为用户改动，不要删除或覆盖，除非用户明确要求。
