# 作品说明文档草稿

作品名称：海报速读行动卡  
参赛赛道：视觉搜索：视觉 + 语音，所见即所想  
版本：草稿版，待根据最终实现更新

## 1. 作品简介

海报速读行动卡是一款面向校园活动场景的视觉理解 AI Agent。用户看到校园活动海报后，只需上传图片或拍照，系统就能自动提取活动名称、时间地点、报名方式和核心亮点，并根据用户身份标签生成个性化参加建议、日程提醒和转发文案。

它解决的不是单纯“识别海报文字”的问题，而是帮助用户从“看见一张海报”快速走到“判断是否值得参加并采取下一步行动”。

## 2. 用户痛点

校园活动海报信息密度高，常常同时包含时间、地点、报名方式、主办方、嘉宾介绍、福利和注意事项。学生在路过公告栏、刷到群聊图片或现场游园时，通常没有耐心逐字阅读，因此容易错过适合自己的活动。

不同用户关心点也不同：技术向用户更关心内容含金量，社交向用户更关心是否适合结识同学，时间紧的用户更关心活动成本和地点距离。传统 OCR 只能读出文字，不能直接完成个性化判断和行动生成。

## 3. 核心功能

| 功能 | 说明 |
|---|---|
| 海报识别 | 从图片中提取活动名称、时间、地点、报名方式、主办方等信息 |
| 身份标签 | 支持技术向、社交向、竞赛向、薅福利、时间紧等身份选择 |
| 个性化推荐 | 根据海报内容和用户标签生成推荐指数、适合原因和准备事项 |
| 行动文案 | 自动生成日程提醒、转发给朋友的话和可分享卡片内容 |
| 兜底演示 | 预置样例和缓存结果保证现场网络或模型异常时仍可展示 |

## 4. 技术方案

系统采用 Web 单页应用作为交互入口，支持用户上传活动海报或选择预置样例。后端接收图片和用户身份标签后，调用多模态模型完成视觉理解、OCR 信息提取和语义归纳，并要求模型返回固定 JSON 结构。前端根据 JSON 渲染结构化摘要、推荐理由和行动卡片。

技术链路：

```text
图片输入
  -> 多模态理解
  -> 活动信息结构化
  -> 结合用户身份做推荐
  -> 生成日程/转发/分享内容
  -> 前端行动卡片展示
```

## 5. AI 能力说明

AI 在作品中的作用包括：

- 视觉理解：识别海报中的文字、图形层级和关键信息。
- 结构化抽取：将分散信息整理为活动名称、时间、地点、报名方式等字段。
- 个性化判断：结合用户身份标签判断活动是否值得参加。
- 生成式行动：生成日程提醒、转发文案和到场准备建议。

这些能力共同服务于一个完整用户旅程，而不是停留在单点识别。

## 6. 用户体验流程

1. 用户进入页面。
2. 上传海报或点击预置样例。
3. 选择身份标签。
4. 系统生成活动摘要和推荐指数。
5. 用户查看适合原因和准备事项。
6. 用户复制日程提醒或转发文案。

## 7. 创新点

- 从“看懂图片”延伸到“生成行动”，让视觉搜索不止于识别。
- 将校园活动海报这一高频但低效率场景做成 30 秒闭环。
- 用身份标签提升推荐差异，让同一张海报对不同用户产生不同结果。
- 通过预置样例和缓存结果兼顾现场可玩性与稳定性。

## 8. 评分点对齐

| 评分点 | 对齐说明 |
|---|---|
| 用户价值 | 降低校园活动理解成本，帮助用户快速做出参与决策 |
| AI 能力与产品结合 | 多模态识别结果直接驱动推荐、日程和分享文案 |
| 体验完整性 | 完成从海报输入到行动输出的闭环 |
| 创意与场景洞察 | 聚焦校园信息过载和活动触达效率问题 |
| 展示稳定性 | 支持预置样例、缓存结果和录屏兜底 |

## 9. 后续拓展

- 接入日历，一键创建活动提醒。
- 支持语音追问，例如“适合大一新生吗”“离我现在的位置远吗”。
- 接入校园活动平台，形成活动发现和推荐入口。
- 支持社团运营者分析海报可读性，反向优化活动传播效果。

